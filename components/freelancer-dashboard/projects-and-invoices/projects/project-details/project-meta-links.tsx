'use client';

type Props = {
  projectId: string | number;
};

export default function ProjectMetaLinks({ projectId: _ }: Props) {
  // Component simplified - redundant links removed
  // Project files are accessible via action buttons
  // Project description is shown in the header
  return null;
}