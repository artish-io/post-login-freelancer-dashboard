{
  "scripts": {
    "check-legacy": "node scripts/check-legacy-usage.js",
    "storage-health": "curl -s http://localhost:3000/api/admin/storage-health | jq",
    "validate-storage": "node scripts/check-legacy-usage.js && npm run storage-health"
  },
  "eslintConfig": {
    "rules": {
      "no-legacy-storage": "error"
    }
  }
}
